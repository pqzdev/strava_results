{
  "name": "strava-club-results",
  "version": "0.1.0",
  "description": "Aggregate race results from running club members' Strava accounts",
  "private": true,
  "workspaces": [
    "workers",
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=workers & npm run dev --workspace=frontend",
    "build": "npm run build --workspace=workers && npm run build --workspace=frontend",
    "deploy": "cd workers && npx wrangler deploy",
    "db:migrate": "wrangler d1 execute strava-club-db --file=database/schema.sql"
  },
  "keywords": [
    "strava",
    "running",
    "race-results",
    "cloudflare-workers"
  ],
  "author": "",
  "license": "MIT"
}
