# This file exists to prevent Cloudflare Pages from failing builds
# The actual Workers configuration is in wrangler.workers.toml
# Pages deployment happens via GitHub Actions, not automatic builds

# Placeholder configuration
name = "placeholder"
compatibility_date = "2024-11-10"
