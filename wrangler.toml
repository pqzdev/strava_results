# This file is a placeholder to prevent build errors
# Actual deployments:
# - Workers: GitHub Actions with wrangler.workers.toml
# - Pages: GitHub Actions with wrangler pages deploy
#
# Cloudflare Pages automatic builds should be DISABLED in the dashboard
# to avoid conflicts with GitHub Actions deployments

name = "placeholder"
compatibility_date = "2024-11-10"
