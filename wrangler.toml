# Cloudflare Pages configuration for automatic builds
# Workers use wrangler.workers.toml (deployed via GitHub Actions)

name = "woodstock-results"
compatibility_date = "2024-11-10"
pages_build_output_dir = "dist"
